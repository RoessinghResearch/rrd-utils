signing.keyId=
signing.password=
signing.secretKeyRingFile=

ossrhUsername=
ossrhPassword=

# This line is needed because Maven Central does not accept the sha256 and
# sha512 files generated by the gradle assemble target.
systemProp.org.gradle.internal.publish.checksums.insecure=true
